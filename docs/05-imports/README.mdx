---
title: "Imports"
description: "React components and npm packages in MDX"
---

import { Counter } from './Counter.tsx'
import confetti from 'canvas-confetti'

<FrontMatter />

# Imports

MDX supports ES imports. Use local components or npm packages.

## Local Components

Create a component next to your MDX:

```tsx
// Counter.tsx
import { useState } from 'react'

export function Counter({ initial = 0 }) {
  const [count, setCount] = useState(initial)
  return (
    <div>
      <button onClick={() => setCount(c => c - 1)}>-</button>
      <span>{count}</span>
      <button onClick={() => setCount(c => c + 1)}>+</button>
    </div>
  )
}
```

Import in MDX:

```mdx
import { Counter } from './Counter.tsx'

<Counter initial={5} />
```

<Counter initial={5} />

## npm Packages

Installed packages work:

```mdx
import confetti from 'canvas-confetti'

<button onClick={() => confetti()}>
  Confetti
</button>
```

<button
  onClick={() => confetti({ particleCount: 100, spread: 70 })}
  className="px-4 py-2 bg-primary text-primary-foreground rounded"
>
  Confetti
</button>

## Import Patterns

```mdx
// Named export
import { MyComponent } from './MyComponent.tsx'

// Default export
import MyComponent from './MyComponent.tsx'

// npm package
import lodash from 'lodash'
```
