---
title: "Writing Content"
description: "MDX syntax and formatting"
---

<FrontMatter />

# Writing Content

veslx uses MDX - markdown that supports JSX. Write markdown or embed React components.

## Frontmatter

Pages start with YAML frontmatter:

```yaml
---
title: "Page Title"
description: "Brief description for listings"
date: "2025-01-15"
draft: true           # Hide from listings
visibility: "hidden"  # Also hides from listings
---
```

Display it with `<FrontMatter />`:

```mdx
<FrontMatter />
```

## Markdown

Standard markdown works:

### Text

**Bold**, *italic*, ~~strikethrough~~, `inline code`.

### Lists

- Unordered item
- Another item
  - Nested item

1. Ordered item
2. Second item

### Blockquotes

> A blockquote.
>
> Can span multiple paragraphs.

### Links

[External link](https://github.com) opens in new tab.
[Internal link](./01-getting-started.mdx) uses client-side navigation.

### Code Blocks

```typescript
function greet(name: string): string {
  return `Hello, ${name}!`;
}
```

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

### Tables

| Feature | Status |
|---------|--------|
| MDX | Supported |
| Math | KaTeX |
| Slides | `---` separators |

---

## File Ordering

Files sort by:

1. **Numeric prefix** - `01-intro.mdx` sorts first, displays as "Intro"
2. **Date** - Files with dates sort newest first
3. **Alphabetical** - Everything else

```
docs/
├── 01-getting-started.mdx  → "Getting Started" (1st)
├── 02-configuration.mdx    → "Configuration" (2nd)
├── my-post.mdx             → Has date, sorts by date
└── zebra.mdx               → Alphabetical
```
